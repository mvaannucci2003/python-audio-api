to run this program, you will need UV on your machine. When that is downloaded run uv sync to get all the dependancies you need to run the API and generate CSV files. You will also 
need a key for both 11labs and gemini. Create a .env file on your machine and uv sync will take care of the rest once the keys are inputted into that file. Then run main.py to 
execute the program.
